# Copy to .env.local to override. In development the Vite server proxies
# /api to http://localhost:8000, so no changes are needed locally.
# For production builds, point at your deployed backend:
VITE_API_URL=http://localhost:8000
